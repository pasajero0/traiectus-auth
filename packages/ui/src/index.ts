/**
 * Components and design tokens shared by every web application — the client
 * apps and the identity service's own sign-in pages alike, so the visual
 * language is one thing rather than three that resemble each other.
 *
 * Also the groundwork for the mobile client in v2, which is why this is a
 * package and not a folder inside one app.
 */

export {}
