import 'server-only'

/**
 * The only thing in this application that talks to sso-api, and the only place
 * the internal API key is read. Nothing here ever reaches the browser.
 *
 * Day 2.
 */

export {}
