NODE_ENV=development
PORT=3100

# Public key the identity service signs access tokens with. Verification is
# local: this service never calls back to sso-api on the request path.
ACCESS_TOKEN_PUBLIC_KEY=

# The one browser origin allowed to call this API.
ALLOWED_ORIGIN=http://localhost:3000
