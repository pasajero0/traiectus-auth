# Exact versions everywhere: `pnpm add x` writes "4.13.5", not "^4.13.5".
# An identity service should be running the dependency tree that was tested,
# not whichever compatible release happened to exist at install time.
save-prefix=''
